# Copy to .env and fill in. Never commit the real .env.
DATABASE_URL=postgres://user:password@localhost:5432/todo?sslmode=disable
PORT=8080
# Set false only for local http dev; production cookies must be Secure.
COOKIE_SECURE=true
# Only read by cmd/seed-user when creating a login.
SEED_PASSWORD=
