# Plan 02 — CategoryDrawerRow

## Goal
Add `src/components/main-list/CategoryDrawerRow.tsx`: a single category list
row used inside the Drawer. Shows label, trailing open-todo count, and an
accent vertical bar + raised background when `active`.

## TDD

### Red
Add `src/components/main-list/CategoryDrawerRow.test.tsx`:
- Renders label + count.
- Active row has `border-l` + `border-accent` + `bg-bgRaised` classes.
- Inactive row does NOT include `border-accent` / `bg-bgRaised`.
- Click fires `onSelect`.
- Has 44pt min-height (class includes `min-h-[44px]`).

Run `npm test -- --run src/components/main-list/CategoryDrawerRow.test.tsx`
→ expected red.

### Green
```tsx
interface CategoryDrawerRowProps {
  label: string;
  count: number;
  active: boolean;
  onSelect: () => void;
}

export default function CategoryDrawerRow({
  label, count, active, onSelect,
}: CategoryDrawerRowProps) {
  const base =
    'w-full flex items-center justify-between px-4 min-h-[44px] text-left text-text';
  const activeCls = active
    ? 'border-l-[3px] border-accent bg-bgRaised'
    : 'border-l-[3px] border-transparent';
  return (
    <button
      type="button"
      onClick={onSelect}
      aria-current={active ? 'page' : undefined}
      className={`${base} ${activeCls}`}
    >
      <span className="text-base font-medium">{label}</span>
      <span className="text-textDim text-sm tabular-nums">{count}</span>
    </button>
  );
}
```

### Refactor
None expected.

## Verification
- `npm test -- --run` green.
- `npm run typecheck` clean.
- `npm run build` clean.

## Commit
`feat(05): add CategoryDrawerRow component`
