# Plan 02 — AppBar component

## Goal
Build `src/components/main-list/AppBar.tsx`: 56pt-tall top bar with hamburger button (left), brand "Todo" (center/left), settings cog (right), and a hairline-bottom divider. Buttons fire `onOpenDrawer` and `onOpenSettings` props.

## TDD

### Red
Add `src/components/main-list/AppBar.test.tsx`:
- Renders three buttons with accessible names: hamburger (`aria-label="Otwórz menu"`), settings (`aria-label="Otwórz ustawienia"`), brand text "Todo".
- Clicking hamburger calls `onOpenDrawer`.
- Clicking settings calls `onOpenSettings`.
- Wrapping element has Tailwind class `border-b` and a `border-hairline` (or `border-hairlineSoft`) class.

Add corresponding i18n keys: `menuOpen: 'Otwórz menu'`, `settingsOpen: 'Otwórz ustawienia'`. Brand stays `t.brand` (overridden to `'Todo'` per spec — see PLAN-04 for full i18n changes; it's fine to update incrementally).

`npm test -- --run src/components/main-list/AppBar.test.tsx` → red (file does not exist).

### Green
Create `src/components/main-list/AppBar.tsx`:
```tsx
interface AppBarProps {
  onOpenDrawer: () => void;
  onOpenSettings: () => void;
}
export default function AppBar({ onOpenDrawer, onOpenSettings }: AppBarProps) {
  return (
    <header className="h-14 flex items-center justify-between px-4 border-b border-hairlineSoft bg-bg">
      <button type="button" aria-label={t.menuOpen} onClick={onOpenDrawer}
              className="w-11 h-11 grid place-items-center text-text">
        {/* hamburger SVG (3 lines) */}
      </button>
      <div className="text-text font-semibold text-lg">{t.brand}</div>
      <button type="button" aria-label={t.settingsOpen} onClick={onOpenSettings}
              className="w-11 h-11 grid place-items-center text-text">
        {/* cog SVG */}
      </button>
    </header>
  );
}
```
SVGs inline; stroke=currentColor, no external icon library.

Update `src/i18n.ts`: add `menuOpen`, `settingsOpen`. Set `brand: 'Todo'` (was `'Todo Witek'`). Note: brand change risks Login test asserting old brand — verify with grep before committing.

### Refactor
If brand-rename breaks login tests, revert brand to `'Todo Witek'` and use a literal `'Todo'` here per spec — the design spec brand string is exactly `Todo`.

## Verification
- `npm test -- --run` all green.
- `npm run typecheck`, `npm run build` clean.

## Commit
`feat(04): add AppBar with hamburger, brand, and settings buttons`
