# Plan 03 — CategoryTabsBar component

## Goal
Replace `CategoryTabs.tsx` with `src/components/main-list/CategoryTabsBar.tsx`: two segmented tabs (Prywatne / Służbowe) with an animated 2px accent underline that translates between active tabs over 180ms ease-in-out.

## TDD

### Red
`src/components/main-list/CategoryTabsBar.test.tsx`:
- Renders both tabs with role=tab and texts `Prywatne`, `Służbowe`.
- The currently selected tab has `aria-selected="true"`; the other `false`.
- Clicking the inactive tab calls `onChange('sluzbowe')` (and vice versa).
- Underline element exists with `data-testid="tab-underline"`, has `transition-[transform,width]` class and a `motion-reduce:transition-none` class.
- Underline element's inline style includes a transform (assert string contains `translateX`) — value differs by active tab. Use `data-active` attribute (`prywatne`/`sluzbowe`) on container for assertion stability.

`npm test -- --run` → red.

### Green
`src/components/main-list/CategoryTabsBar.tsx`:
- Container relative; both tabs flex-1 buttons.
- Single absolutely-positioned `<span data-testid="tab-underline">` at bottom: `bottom-0 h-0.5 bg-accent transition-[transform,width] duration-180 ease-in-out motion-reduce:transition-none`.
- Inline style: `style={{ width: '50%', transform: value === 'prywatne' ? 'translateX(0%)' : 'translateX(100%)' }}`.
- Each tab button: `flex-1 h-11 text-base font-medium ${selected ? 'text-text' : 'text-textDim'}`.

Props:
```ts
interface CategoryTabsBarProps {
  value: TodoCategory;
  onChange: (next: TodoCategory) => void;
}
```

Tailwind v4 doesn't ship `duration-180` by default — use `[transition-duration:180ms]` arbitrary utility, or arbitrary `[transition:transform_180ms_ease-in-out,width_180ms_ease-in-out]`. Pick one and assert it.

### Refactor
None.

## Verification
- `npm test -- --run` green.
- `npm run typecheck`, `npm run build` clean.

## Commit
`feat(04): add CategoryTabsBar with animated accent underline`
