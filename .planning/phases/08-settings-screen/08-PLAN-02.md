# Plan 02 — SettingsGroup card + AccountGroup

## Goal
- `src/components/settings/SettingsGroup.tsx` — visual container:
  `bg-bgRaised rounded-group p-4`, optional `title` rendered as a
  small uppercase overline above children.
- `src/components/settings/AccountGroup.tsx` — shows `Zalogowany jako`
  + email; trailing text-button `Wyloguj` in `text-danger`. Hidden when
  `email` is null (local mode signal).

## TDD

### Red — `SettingsGroup.test.tsx`
- Renders children inside a `bg-bgRaised`/`rounded-group` container.
- Renders `title` when provided.
- No title element when `title` omitted.

### Red — `AccountGroup.test.tsx`
- Renders email and Wyloguj button when `email` provided.
- Returns `null` when `email` is `null`.
- Click on Wyloguj calls `onSignOut`.
- Wyloguj button has `text-danger` class (smoke test for spec).

Run: red.

### Green
Implement both components per CONTEXT pseudocode. AccountGroup props:
`{ email: string | null; onSignOut: () => void }`.

### Refactor
SettingsGroup keeps zero business logic. AccountGroup early-returns
on null email — no conditional rendering inside SettingsSheet later.

## Verification
- Tests green, typecheck + build clean.

## Commit
`feat(08): SettingsGroup container + AccountGroup`
