---
milestone: v1.0
milestone_name: Core Feature Completion
audited: 2026-04-28T12:30:00Z
status: passed
scores:
  requirements: 9/9
  phases: 2/2
  integration: 12/12
  flows: 4/4
gaps: []
tech_debt:
  - phase: 01-push-notifications-setup
    items:
      - "LocalApp still has legacy requestNotificationPermission() call in bare useEffect — accepted behavior per CONTEXT.md decision."
      - "Human verification deferred: live FCM opt-in flow, foreground banner, opt-out Firestore delete, dist artifact check require a real browser + VAPID key + live Firebase project."
  - phase: 02-firestore-api-github-actions-reminder-cron
    items:
      - "App.tsx error banner uses (error as { code?: string }) cast rather than instanceof FirebaseError — works at runtime but not type-safe. Low severity."
      - "Human verification deferred: FIREBASE_SERVICE_ACCOUNT_KEY secret must be set in GitHub repo settings; cron workflow_dispatch should be triggered manually to confirm end-to-end delivery in CI."
      - "vi.mock() unusable in .cjs Vitest test files (Vitest is ESM-only) — dependency injection used instead. Documented in 02-02-SUMMARY."
nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases: [01-push-notifications-setup, 02-firestore-api-github-actions-reminder-cron]
  overall: MISSING
---

# Milestone v1.0 — Core Feature Completion — Audit Report

**Audited:** 2026-04-28T12:30:00Z
**Status:** passed
**Phases:** 2/2 complete
**Requirements:** 9/9 satisfied
**Integration:** 12/12 cross-module links verified
**E2E Flows:** 4/4 complete

---

## Requirements Coverage

No formal REQUIREMENTS.md. Requirements derived from ROADMAP.md phase goals and verified through 3-source cross-reference: VERIFICATION.md, SUMMARY.md, and integration checker report.

| REQ-ID | Description | Phase | VERIFICATION.md | Integration | Final Status |
|--------|-------------|-------|-----------------|-------------|--------------|
| REQ-01 | FCM token registration | 01 | VERIFIED (5 tests) | WIRED | **satisfied** |
| REQ-02 | Permission request UI | 01 | VERIFIED (8+12 tests) | WIRED | **satisfied** |
| REQ-03 | Firestore token storage | 01 | VERIFIED (5 tests) | WIRED | **satisfied** |
| REQ-04 | Foreground message handling | 01 | VERIFIED (tests 10–11) | WIRED | **satisfied** |
| REQ-05 | Background message handling (SW stamping) | 01 | VERIFIED (build passes) | WIRED | **satisfied** |
| REQ-06 | Per-device opt-in/opt-out | 01 | VERIFIED (8 tests) | WIRED | **satisfied** |
| REQ-07 | Auto-permission removed from FirebaseApp | 01 | VERIFIED (1 regression test) | WIRED | **satisfied** |
| REQ-08 | Firestore permission-denied shown as actionable banner | 02 | VERIFIED (2 new tests) | WIRED | **satisfied** |
| REQ-09 | GitHub Actions cron sends FCM for due reminders every 15 min | 02 | VERIFIED (5 integration tests) | WIRED | **satisfied** |

**Score: 9/9** — no unsatisfied, no orphaned requirements.

---

## Phase Verification Summary

| Phase | Plans | Tests | Verification Status | Notes |
|-------|-------|-------|---------------------|-------|
| 01 — Push Notifications Setup | 6/6 complete | 110 pass (17 files) | human_needed (12/13 automated truths) | Live FCM device test deferred |
| 02 — Firestore API + GitHub Actions Cron | 2/2 complete | 110 pass + 5 emulator tests | passed | FIREBASE_SERVICE_ACCOUNT_KEY setup deferred to user |

---

## Integration Findings

Integration checker verified 12 cross-module links across both phases:

**Phase 01 (intra-phase):**

| Integration Point | Status | Details |
|---|---|---|
| usePushNotifications → pushTokens | WIRED | getCurrentDeviceToken, registerCurrentDeviceForPush, unregisterDeviceToken connected |
| usePushNotifications → messaging | WIRED | onForegroundMessage with StrictMode-safe cancellation guard |
| App.tsx → usePushNotifications | WIRED | usePushNotifications(user.uid) inside FirebaseAuthenticated |
| App.tsx → Shell (pushBanner) | WIRED | push.bannerMessage passed as pushBanner prop |
| Shell → banner div | WIRED | Rendered before offline/error banners |
| vite.config.ts → stampFirebaseSwPlugin | WIRED | writeBundle stamps dist/firebase-messaging-sw.js at build time |

**Phase 02 (intra-phase and cross-phase):**

| Integration Point | Status | Details |
|---|---|---|
| fcmTokens schema: Phase 01 write → Phase 02 read | WIRED | Phase 01 writes `{ userId, token, createdAt }` with token as doc ID; Phase 02 queries `where('userId','==',ownerId)` and maps `.data().token` — exact field match |
| todos schema: app → cron | WIRED | `Reminder { id, remindAt, fired }` in types.ts matches exactly what send-reminders.cjs reads and writes |
| Stale token cleanup path | WIRED | Phase 01 writes doc ID = token string; Phase 02 deletes `fcmTokens.doc(tokenList[idx])` by same token-string ID |
| firestoreNotEnabled error chain | WIRED | observeUserTodos onError → useTodos setError → Shell `error.code === 'permission-denied'` → t.firestoreNotEnabled render |
| Mark-fired write-back prevents duplicate local notifications | WIRED | Phase 02 sets `fired: true` in Firestore → observeUserTodos subscription updates → useReminderScheduler.sync(todos) skips fired reminders |
| GitHub Actions → send-reminders.cjs | WIRED | `node scripts/send-reminders.cjs` with `FIREBASE_SERVICE_ACCOUNT_KEY` env var |

---

## E2E Flows

| Flow | Steps | Status |
|------|-------|--------|
| Push opt-in | User logs in → clicks Enable push → grants permission → token registered in Firestore `fcmTokens` | WIRED (live test deferred) |
| Foreground banner | Push arrives → onForegroundMessage fires → bannerMessage set → Shell renders banner → auto-dismisses 5s | WIRED (live test deferred) |
| Push opt-out | User clicks Disable → unregisterDeviceToken → Firestore doc deleted → UI transitions | WIRED (live test deferred) |
| Cross-device reminder cron | User creates todo with reminder → Firestore write → cron reads due reminders → FCM multicast → fired=true written back | WIRED (requires FIREBASE_SERVICE_ACCOUNT_KEY secret + live Firebase project) |

---

## Tech Debt

| Phase | Item | Severity | Notes |
|-------|------|----------|-------|
| 01 | LocalApp legacy auto-permission call | Low | Accepted behavior per CONTEXT.md |
| 01 | Live FCM device tests deferred | Low | VAPID key configured; run against deployed app |
| 02 | `(error as { code?: string })` cast in App.tsx | Low | Works at runtime; `instanceof FirebaseError` would be more type-safe |
| 02 | FIREBASE_SERVICE_ACCOUNT_KEY GitHub secret not yet set | Medium | User action required before cron runs in CI |

---

## Nyquist Compliance

| Phase | VALIDATION.md | Status |
|-------|---------------|--------|
| 01 — Push Notifications Setup | Missing | MISSING |
| 02 — Firestore API + GitHub Actions Cron | Present (02-VALIDATION.md) | Exists but not formally parsed |

Nyquist validation was not run as part of this milestone. Discovery only — not blocking.

---

## Anti-Patterns

No blockers. No TODO/FIXME/placeholder comments in phase files. No empty stubs in production code paths. TypeScript strict mode clean. All tests pass.

---

_Audited: 2026-04-28T12:30:00Z_
_Auditor: Claude (gsd-audit-milestone)_
