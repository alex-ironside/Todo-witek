# Plan 05 — SettingsSheet composer + MainList wiring + cleanup

## Goal
- `src/components/settings/SettingsSheet.tsx` — composes all groups in
  the Phase 7 `Sheet` primitive. Title `Ustawienia`. `space-y-4`
  between groups.
- `MainList.tsx` — owns `settingsOpen` state, wires AppBar
  `onOpenSettings` to open it. Consumes `useAccent` and renders
  `<SettingsSheet>` with all needed props.
- `App.tsx` — passes `email`, `mode`, `setMode`, `push`, `signOut`,
  `pwaInstall` props down to MainList. Removes the temporary footer
  strip and the three deleted components' imports.
- Delete: `src/components/StorageModeToggle.tsx`,
  `src/components/PushToggle.tsx`, `src/components/PushToggle.test.tsx`,
  `src/components/InstallButton.tsx`.
- Update `src/App.test.tsx`: drop the three component mocks; un-mock
  MainList (or keep mock and add a settings-rendering branch). Open
  settings sheet via the cog click to keep the signOut regression test
  intact.

## TDD

### Red — `SettingsSheet.test.tsx`
- Renders Sheet titled `Ustawienia` when `open=true`.
- Sheet hidden (translate-y-full) when `open=false`.
- AccountGroup hidden when `email` is null; visible when set.
- PushGroup hidden when `push` is null; visible when set with valid
  status.
- AppearanceGroup, StorageGroup, InstallGroup always rendered when
  reachable (their internal hide rules tested in their own files).
- Esc closes (delegated to Sheet, smoke test).

### Red — extend `MainList.test.tsx`
- Clicking the settings cog renders the settings sheet with title
  `Ustawienia`.
- Mock `useAccent` to return `['amber', vi.fn()]`.
- Mock `useStorageMode` to return `['local', vi.fn()]`.
- With no `email`/`push` props (defaults), AccountGroup + PushGroup
  not rendered (smoke test).

### Red — update `App.test.tsx`
- Remove `vi.mock('./components/StorageModeToggle' ...)` etc.
- Un-mock MainList. The signOut regression test now opens settings via
  cog click and clicks Wyloguj inside the sheet.
- Or: keep MainList mocked and have App.tsx still own a hidden signOut
  trigger — preferred path is un-mock for fidelity.

Run: red.

### Green
- Build SettingsSheet composer.
- Update MainList signature: add optional props `email?: string | null`,
  `push?: PushState | null`, `onSignOut?: () => void`,
  `pwaInstall?: { canInstall: boolean; promptInstall: () => Promise<void> }`.
  Use `useAccent` and `useStorageMode` inline.
- Update App.tsx: delete temporary footer; pass props down. Move
  `usePushNotifications` into `FirebaseAuthenticated` (already lives
  there). Keep `signOut` lambda (push.disable then logout) and pass it
  into MainList.
- Delete the three components + their tests.

### Refactor
- Verify no remaining imports of deleted components
  (`grep -r StorageModeToggle\|PushToggle\|InstallButton src/`).
- Ensure App.test.tsx mocks reference only the modules that still
  exist.

## Verification
- Full test suite green.
- `npm run typecheck` clean.
- `npm run build` clean.

## Commit
`feat(08): SettingsSheet composer + MainList wiring + remove legacy toggles`
