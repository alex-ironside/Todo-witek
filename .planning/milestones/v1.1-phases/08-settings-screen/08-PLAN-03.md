# Plan 03 — AppearanceGroup (accent picker) + StorageGroup

## Goal
- `src/components/settings/AppearanceGroup.tsx` — 5-swatch picker
  bound to `[accent, setAccent]` props (hook stays in MainList).
  Active swatch has `ring-2 ring-accent ring-offset-2 ring-offset-bgRaised`.
  Each swatch uses inline `style={{ backgroundColor: ACCENTS[k].oklch }}`
  (the only non-Tailwind escape; documented in CONTEXT).
- `src/components/settings/StorageGroup.tsx` — Segmented Lokalnie /
  Chmura bound to `[mode, setMode]` props (`'local' | 'firebase'`).

## TDD

### Red — `AppearanceGroup.test.tsx`
- Renders 5 swatch buttons with Polish aria-labels (Bursztyn, Róż,
  Mięta, Fiolet, Błękit) sourced from `ACCENTS[key].label`.
- Active swatch (matching `accent` prop) has ring class.
- Click on a non-active swatch calls `setAccent(key)`.
- Each swatch's inline style includes its OKLCH string.

### Red — `StorageGroup.test.tsx`
- Renders both segments with labels Lokalnie / Chmura.
- Active segment matches `mode` prop.
- Clicking the inactive segment calls `setMode` with the right
  internal value (`'local'` or `'firebase'`).

Run: red.

### Green
Implement both. AppearanceGroup wraps content in `SettingsGroup`
with `title={t.appearance}`. StorageGroup uses
`title={t.storage}`.

### Refactor
Pull the swatch list from `ACCENT_KEYS`. Don't hard-code 5 entries.

## Verification
- Tests green, typecheck + build clean.

## Commit
`feat(08): AppearanceGroup accent picker + StorageGroup segmented`
