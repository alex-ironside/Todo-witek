# Plan 01 — Switch + Segmented primitives + i18n keys

## Goal
Add the small interaction primitives the settings groups consume:
- `src/components/settings/Switch.tsx` — controlled toggle, 36×20 track
  in `bg-bgRaised`, 16×16 knob in `bg-accent`, 200ms slide.
- `src/components/settings/Segmented.tsx` — generic 2-option segmented
  pill on `bg-bg` with active `bg-bgRaised` segment.
- Extend `src/i18n.ts` with the new keys (additive only).

## TDD

### Red — `Switch.test.tsx`
- Renders with `role="switch"` and `aria-checked` reflecting `checked`.
- Click calls `onChange` with `!checked`.
- Disabled prop blocks `onChange`.

### Red — `Segmented.test.tsx`
- Renders both options as `role="tab"`-like buttons with `aria-pressed`
  reflecting `value`.
- Clicking inactive option fires `onChange(value)`.
- Clicking active option does not refire.
- Generic over the value type (string union).

### Red — `i18n.test.ts` (extend or add)
- New keys present: `signedInAs`, `appearance`, `accentLabel`,
  `storage`, `pushTitle`, `pushHint`, `pushOn`, `pushOff`, `installApp`
  (already exists — keep), `installHint`. Verify Polish strings match
  the README handoff table.
- Existing keys unchanged: `signOut`, `modeLocal`, `modeCloud`,
  `settings`.

Run: `npm test -- --run` → red on the new files.

### Green
- Implement `Switch` with `<button role="switch">` and Tailwind
  classes; knob translates with `transition-transform duration-200`.
- Implement `Segmented<T extends string>` taking `value`, `onChange`,
  and `options: { value: T; label: string }[]`.
- Add the missing i18n keys verbatim from the design handoff Copy
  table (Polish source of truth).

### Refactor
Doc headers note the OKLCH constraint: knob uses `bg-accent` (CSS
var) so accent changes propagate without per-swatch styles.

## Verification
- `npm test -- --run` green (340+ tests).
- `npm run typecheck` clean.
- `npm run build` clean.

## Commit
`feat(08): Switch + Segmented primitives + settings i18n`
