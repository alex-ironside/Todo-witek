# Plan 01 — Tailwind v4 install + OKLCH @theme tokens

## Goal
Install Tailwind v4 with `@tailwindcss/vite` and replace `src/index.css` with an `@theme` block that declares all OKLCH tokens, radii, and font-family from the locked spec. Verify token presence by reading the file (jsdom does not process Tailwind / `@theme`, and `vite.config.ts` has `css: false` for tests, so a runtime `getComputedStyle` smoke is not viable; a source-file smoke is).

## TDD

### Red
Add `src/index.css.test.ts` (alongside `src/index.css`) that reads the file and asserts:
- contains `@import "tailwindcss";`
- contains `@theme {`
- declares each color token: `--color-bg`, `--color-bgRaised`, `--color-bgSheet`, `--color-hairline`, `--color-hairlineSoft`, `--color-text`, `--color-textDim`, `--color-textMute`, `--color-accent`, `--color-accentInk`, `--color-danger`
- each color uses `oklch(`
- declares radii: `--radius-field: 12px`, `--radius-group: 16px`, `--radius-sheet: 24px`
- declares `--font-sans` starting with `Inter`

Run `npm test -- --run src/index.css.test.ts` → expected red (current file is the legacy theme).

### Green
- Install `tailwindcss@^4` and `@tailwindcss/vite@^4`.
- Add the plugin to `vite.config.ts` (before VitePWA).
- Rewrite `src/index.css` per CONTEXT.md decisions:
  ```css
  @import "tailwindcss";

  @theme {
    --color-bg: oklch(0.18 0.008 60);
    --color-bgRaised: oklch(0.22 0.009 60);
    --color-bgSheet: oklch(0.24 0.010 60);
    --color-hairline: oklch(0.30 0.008 60);
    --color-hairlineSoft: oklch(0.26 0.008 60);
    --color-text: oklch(0.96 0.005 80);
    --color-textDim: oklch(0.72 0.012 70);
    --color-textMute: oklch(0.55 0.010 70);
    --color-accent: oklch(0.78 0.13 70);
    --color-accentInk: oklch(0.20 0.020 60);
    --color-danger: oklch(0.65 0.18 25);

    --radius-field: 12px;
    --radius-group: 16px;
    --radius-sheet: 24px;

    --font-sans: Inter, system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
  }

  html, body { background: var(--color-bg); color: var(--color-text); margin: 0; }
  body { font-family: var(--font-sans); min-height: 100vh; }
  ```

### Refactor
Drop legacy CSS rules entirely. Existing components will look broken — accepted per CONTEXT.md.

## Verification
- `npm test -- --run` (the new smoke test passes; `App.test.tsx` asserts behavior, not class names — should remain green).
- `npm run typecheck` passes.
- `npm run build` passes (Tailwind plugin builds, no missing utility errors because no app code yet uses Tailwind classes).

## Commit
`feat(03): install Tailwind v4 and lock OKLCH theme tokens`
