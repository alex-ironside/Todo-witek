---
phase: 260508-gp8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/main-list/MoveCategorySheet.tsx
  - src/components/main-list/MoveCategorySheet.test.tsx
  - src/components/main-list/MainList.tsx
  - src/components/main-list/MainList.test.tsx
autonomous: false
requirements:
  - QUICK-260508-gp8
must_haves:
  truths:
    - User opens "Przenieś do kategorii" sheet from a todo overflow menu and sees a "Zarządzaj kategoriami" button at the bottom.
    - Clicking that button closes the move sheet and opens the existing ManageCategoriesSheet (the same one the sidebar drawer opens).
    - No duplicate management UI is introduced — clicking the new button reuses the same ManageCategoriesSheet instance / handler that the sidebar uses.
    - When the categoryRepo is unavailable (no manage handler wired), the button is not rendered (parity with the drawer's conditional behavior).
  artifacts:
    - path: src/components/main-list/MoveCategorySheet.tsx
      provides: Renders an optional "Zarządzaj kategoriami" footer button when `onManageCategories` prop is provided.
      contains: "onManageCategories"
    - path: src/components/main-list/MainList.tsx
      provides: Wires MoveCategorySheet's `onManageCategories` to the same `setManageOpen(true)` handler used by Drawer; closes the move sheet first.
      contains: "onManageCategories"
  key_links:
    - from: src/components/main-list/MoveCategorySheet.tsx
      to: src/components/main-list/MainList.tsx
      via: onManageCategories prop
      pattern: "onManageCategories"
    - from: src/components/main-list/MainList.tsx
      to: src/components/main-list/ManageCategoriesSheet.tsx
      via: setManageOpen(true) (shared with Drawer)
      pattern: "setManageOpen\\(true\\)"
---

<objective>
Add a "Zarządzaj kategoriami" button to the bottom of the MoveCategorySheet ("Przenieś do kategorii"). Clicking it must close the move sheet and open the existing ManageCategoriesSheet — the same one the sidebar Drawer opens. No duplicate management UI.

Purpose: Users currently can only reach category management from the sidebar drawer. While moving a todo, they may realize they need to create/rename/delete a category. Surfacing the same entry point in the move sheet removes a navigation hop.

Output: Updated MoveCategorySheet with optional `onManageCategories` prop and footer button; updated MainList wiring; failing-then-passing integration tests; Playwright MCP verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/types.ts
@src/i18n.ts
@src/components/main-list/MoveCategorySheet.tsx
@src/components/main-list/MoveCategorySheet.test.tsx
@src/components/main-list/Drawer.tsx
@src/components/main-list/ManageCategoriesSheet.tsx
@src/components/main-list/MainList.tsx

<interfaces>
<!-- Existing relevant contracts the executor must preserve. -->

From src/i18n.ts (already exists, reuse — do NOT add a new key):
```ts
manageCategories: 'Zarządzaj kategoriami', // line 15
moveToCategoryTitle: 'Przenieś do kategorii', // line 117
```

From src/components/main-list/MoveCategorySheet.tsx (current props — to be extended):
```ts
interface MoveCategorySheetProps {
  todo: Todo | null;
  categories: Category[];
  onClose: () => void;
  onMove: (todoId: string, categoryId: string) => void | Promise<void>;
  // NEW (optional): onManageCategories?: () => void;
}
```

From src/components/main-list/MainList.tsx (existing wiring around lines 332-369):
- `setManageOpen(true)` is the function the Drawer uses to open ManageCategoriesSheet.
- It is gated on `categoryRepo` being defined (Drawer passes `undefined` when no repo).
- `setMoveForId(null)` closes the MoveCategorySheet.
- The same gating must apply to the new MoveCategorySheet button: only pass `onManageCategories` when `categoryRepo` is defined.

From src/components/main-list/Drawer.tsx (pattern to mirror, lines 71-74):
```ts
const handleManage = () => {
  onManageCategories();
  onClose();
};
```
The new MoveCategorySheet button must follow this exact pattern: call manage handler, then close itself. MainList's handler will then call `setManageOpen(true)` after `setMoveForId(null)` — order matters because both sheets share the screen.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional onManageCategories prop and footer button to MoveCategorySheet (RED → GREEN)</name>
  <files>src/components/main-list/MoveCategorySheet.tsx, src/components/main-list/MoveCategorySheet.test.tsx</files>
  <behavior>
    Extend MoveCategorySheet with an optional `onManageCategories?: () => void` prop. New tests in MoveCategorySheet.test.tsx (added before implementation, must fail first):
    - Test A: When `onManageCategories` is NOT provided, no button with the text "Zarządzaj kategoriami" is rendered inside the sheet.
    - Test B: When `onManageCategories` IS provided and a todo is set, a button with text "Zarządzaj kategoriami" is rendered at the bottom of the sheet (after the category list).
    - Test C: Clicking that button calls `onManageCategories` exactly once AND calls `onClose` exactly once. It does NOT call `onMove`.
    - Test D (regression): existing tests for category-pick behavior still pass — the new button must not interfere with category row clicks.

    All four tests must initially fail (Test A passes trivially with current code; Tests B and C must fail because the prop and button don't exist; ensure the test file imports compile and Test B/C are the meaningful RED).
  </behavior>
  <action>
    1. RED: Edit `src/components/main-list/MoveCategorySheet.test.tsx` and add the new tests (B, C, and an explicit A that asserts absence). Run `npm test -- MoveCategorySheet` and confirm tests B and C fail for the right reason (button not in DOM / prop not wired).
    2. GREEN: Edit `src/components/main-list/MoveCategorySheet.tsx`:
       - Add optional prop: `onManageCategories?: () => void;`
       - After the `<ul>` of categories (still inside the `Sheet`, only when `todo !== null`), render a footer button when `onManageCategories` is defined:
         ```tsx
         {onManageCategories && (
           <button
             type="button"
             onClick={() => { onManageCategories(); onClose(); }}
             className="w-full flex items-center justify-center min-h-[48px] mt-2 border-t border-hairlineSoft text-text font-medium"
           >
             {t.manageCategories}
           </button>
         )}
         ```
       - Reuse `t.manageCategories` from `src/i18n.ts` — do NOT add a new i18n key.
       - The button MUST close the sheet via `onClose()` after invoking `onManageCategories()` (mirrors Drawer.tsx's `handleManage`). Order: invoke manage handler first, then close — so MainList can sequence `setMoveForId(null)` and `setManageOpen(true)` correctly.
    3. Run `npm test -- MoveCategorySheet` — all tests pass. Run `npm run typecheck`.
  </action>
  <verify>
    <automated>cd /home/alex/Documents/priv/Todo-witek && npm test -- MoveCategorySheet --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    - MoveCategorySheet.tsx exposes an optional `onManageCategories` prop.
    - When provided, a "Zarządzaj kategoriami" button renders at the bottom of the open sheet; when omitted, it does not render.
    - Clicking the button invokes both `onManageCategories` and `onClose` (in that order); does not invoke `onMove`.
    - All MoveCategorySheet tests pass; typecheck passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire MoveCategorySheet's onManageCategories to MainList's existing manage handler (RED → GREEN)</name>
  <files>src/components/main-list/MainList.tsx, src/components/main-list/MainList.test.tsx</files>
  <behavior>
    Add an integration test in `MainList.test.tsx` (RED first) that:
    - Renders MainList with a categoryRepo configured (mirror existing MainList.test.tsx setup that already verifies the drawer opens ManageCategoriesSheet — see lines 264-296 for the pattern).
    - Opens a todo's overflow menu and clicks "Przenieś do kategorii" to surface the MoveCategorySheet (existing test pattern at lines 481-498).
    - Asserts the new "Zarządzaj kategoriami" button is present inside the move sheet.
    - Clicks it. Asserts: (a) the MoveCategorySheet is now closed (its dialog is in the offscreen `translate-y-full` state OR no longer queryable in the open dialog form), AND (b) the ManageCategoriesSheet is now open (queryable as a dialog with name "Zarządzaj kategoriami", reusing the assertion shape from the drawer test at line 271).

    Then implement: in MainList.tsx, pass `onManageCategories` to MoveCategorySheet, gated on `categoryRepo` (matching the Drawer's gating). The handler must call `setMoveForId(null)` first (close move sheet), then `setManageOpen(true)` — order matters for animation/focus.
  </behavior>
  <action>
    1. RED: Add the integration test described above to `MainList.test.tsx`. Run it with `npm test -- MainList.test --run` and confirm it fails because MoveCategorySheet receives no `onManageCategories` prop yet.
    2. GREEN: In `src/components/main-list/MainList.tsx`, modify the `<MoveCategorySheet ... />` JSX (around lines 356-361):
       ```tsx
       <MoveCategorySheet
         todo={moveForTodo}
         categories={categories}
         onClose={() => setMoveForId(null)}
         onMove={handleMoveSelect}
         onManageCategories={
           categoryRepo
             ? () => {
                 setMoveForId(null);
                 setManageOpen(true);
               }
             : undefined
         }
       />
       ```
       Reuses the existing `setManageOpen` state setter — does NOT introduce a second ManageCategoriesSheet. The `categoryRepo`-gating mirrors the Drawer's `onManageCategories` (line 340-342) so behavior parity is preserved when no repo is wired.
    3. Run full test suite: `npm test -- --run`. Run `npm run typecheck`. Run `npm run build`.
  </action>
  <verify>
    <automated>cd /home/alex/Documents/priv/Todo-witek && npm test -- --run 2>&1 | tail -30 && npm run typecheck 2>&1 | tail -10 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - MainList passes `onManageCategories` to MoveCategorySheet only when `categoryRepo` is defined.
    - The handler closes the move sheet (`setMoveForId(null)`) then opens the management sheet (`setManageOpen(true)`).
    - New integration test in MainList.test.tsx passes; full test suite green; typecheck clean; build succeeds.
    - No duplicate ManageCategoriesSheet instance was created — the existing one (line 362) is reused.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Playwright MCP end-to-end verification</name>
  <what-built>
    A "Zarządzaj kategoriami" button at the bottom of the "Przenieś do kategorii" sheet that closes the move sheet and opens the existing category management sheet.
  </what-built>
  <how-to-verify>
    Per CLAUDE.md "End-to-end verification — Playwright MCP, always", before declaring done:
    1. Ensure `npm run dev` is running (start it if not).
    2. Use `mcp__playwright__browser_navigate` to load the app, sign in.
    3. Create or pick a todo. Open its overflow menu (`mcp__playwright__browser_click`) and click "Przenieś do kategorii".
    4. `mcp__playwright__browser_snapshot` — confirm the move sheet is open and a "Zarządzaj kategoriami" button is visible at the bottom.
    5. Click "Zarządzaj kategoriami". Snapshot again — confirm the move sheet has closed and the category management sheet is now open (same UI the sidebar drawer opens).
    6. Edge case: in the management sheet, create a new category, then close it. Reopen "Przenieś do kategorii" on the same todo and confirm the new category appears in the list.
    7. Check `mcp__playwright__browser_console_messages` for unexpected errors.
    8. Reproduce sidebar parity: open the drawer, click its "Zarządzaj kategoriami" button, confirm the SAME management sheet opens (visual parity, not a duplicate component).
  </how-to-verify>
  <resume-signal>Type "approved" once the move-sheet button opens the same management UI as the drawer button, with no console errors, or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `npm test -- --run` — all tests green, including new MoveCategorySheet tests and the new MainList integration test.
- `npm run typecheck` — clean.
- `npm run build` — succeeds.
- Playwright MCP flow above — passes.
- Grep audit: `grep -n "ManageCategoriesSheet" src/components/main-list/MainList.tsx` returns exactly ONE rendering site (the existing one near line 362). The fix lifts no state and creates no second instance.
</verification>

<success_criteria>
- "Przenieś do kategorii" sheet shows a "Zarządzaj kategoriami" button at its bottom (when category management is available).
- Clicking it closes the move sheet and opens the existing ManageCategoriesSheet — same component, same handler, same state as the sidebar entry.
- No new i18n keys, no duplicate sheet, no broken existing tests.
- 100% coverage on new code (every new line is exercised by Task 1 and Task 2 tests).
- Playwright MCP verification passed (golden path + new-category edge case + drawer parity).
</success_criteria>

<output>
After completion, create `.planning/quick/260508-gp8-add-manage-categories-button-to-move-to-/260508-gp8-SUMMARY.md`
</output>
